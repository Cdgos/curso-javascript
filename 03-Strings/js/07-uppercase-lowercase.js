// Convertir a mayusculas y minusculas.

// .toUpperCase() => Convierte a mayusculas.
const producto = "Monitor 20 pulgadas";
console.log(producto);
console.log(producto.toUpperCase());

// .toLowerCase() => Convierte a minusculas.
console.log(producto.toLowerCase());

const email = 'SCASADIEGO4@GMAIL.COM'
console.log(email.toLowerCase()) // Convertir un email formulario, lo cual es incorrecto si ponen mayusculas.

// Convertir numero a string.
const precio = 300;
console.log(precio)
console.log(precio.toString())